apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: xyzconfig-order
  description: newconfig Order
  tags:
    - csharp
    - dotnet
spec:
  type: service
  lifecycle: newconfig
  owner: user:guest
  system: xyzconfig-playback
